<%-include('./partials/header')  %> 
    <div class="container">
        <h2><%-post.title  %> </h2>
        <p><%-post.body  %> </p>
        <form action="/<%=post.uuid%>?_method=DELETE" method="POST">
            <button type="submit" class="btn btn-danger my-2">
                delete
            </button>
        </form>
        <a href="/post/<%-post.uuid%> ">
            <button class="btn btn-primary my-2">
                update
            </button>
        </a>

    </div>

<%-include('./partials/footer')  %> 